const fs = require("fs").promise

function renameFile = new promise(function(oldName, newName){
    let newName = myStore;
    if(newName === myStore){
        console.log("name renamed successfully")
    }else{
        console.log("trouble renaming file");
        rejects(Error("trouble renaming file"))
    } 
}) 
